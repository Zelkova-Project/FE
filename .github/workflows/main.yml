name: Hello world
on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Run pwd # 동작명
      run: pwd # 실제 동작
    - name: Run ls -al
      run: ls -al
